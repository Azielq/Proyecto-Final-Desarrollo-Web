@{
    ViewBag.Title = "Acceso Denegado - FarmaU";
}

<div class="container pt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="bi bi-exclamation-triangle-fill me-2"></i>Acceso Denegado</h4>
                </div>
                <div class="card-body text-center py-5">
                    <i class="bi bi-lock-fill text-danger" style="font-size: 5rem;"></i>
                    <h2 class="mt-4">No tienes permisos para acceder a esta página</h2>
                    <p class="lead mb-4">Tu perfil de usuario no cuenta con los privilegios necesarios para acceder al recurso solicitado.</p>
                    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
                        <i class="bi bi-house-door-fill me-2"></i>Volver al inicio
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>