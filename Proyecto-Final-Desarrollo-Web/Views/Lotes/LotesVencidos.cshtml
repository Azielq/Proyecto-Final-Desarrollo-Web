@{
    ViewBag.Title = "Lotes Vencidos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <!-- Migas de pan -->
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Inicio</a></li>
            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Lotes")">Lotes</a></li>
            <li class="breadcrumb-item active" aria-current="page">Lotes Vencidos</li>
        </ol>
    </nav>

    <div class="card">
        <div class="card-header bg-danger text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="fas fa-calendar-times me-2"></i>Lotes Vencidos</h5>
            <div>
                <a href="@Url.Action("Index", "Lotes", new { soloVencidos = true })" class="btn btn-light btn-sm">
                    <i class="fas fa-search me-1"></i>Ver Detalle
                </a>
                <a href="@Url.Action("Index", "Lotes")" class="btn btn-secondary btn-sm">
                    <i class="fas fa-arrow-left me-1"></i>Volver
                </a>
            </div>
        </div>
        <div class="card-body">
            <div class="alert alert-danger">
                <i class="fas fa-exclamation-circle me-2"></i>
                <strong>¡Atención!</strong> Los lotes vencidos representan productos que no deberían utilizarse y podrían representar un riesgo.
            </div>

            <!-- Panel de información de vencimientos -->
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h6 class="mb-0"><i class="fas fa-info-circle me-2"></i>Consejos para gestionar lotes vencidos</h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <h6><i class="fas fa-check-circle text-success me-1"></i>Acciones recomendadas</h6>
                            <ul>
                                <li>Marcar claramente los productos vencidos como tales</li>
                                <li>Separar físicamente los productos vencidos del resto del inventario</li>
                                <li>Gestionar la disposición adecuada según las normativas de productos farmacéuticos</li>
                                <li>Documentar el proceso de eliminación para mantener registros transparentes</li>
                                <li>Revisar regularmente los lotes próximos a vencer para evitar acumulación</li>
                            </ul>
                        </div>
                        <div class="col-md-6">
                            <h6><i class="fas fa-times-circle text-danger me-1"></i>Prácticas a evitar</h6>
                            <ul>
                                <li>Mantener los productos vencidos mezclados con productos válidos</li>
                                <li>Dispensar o vender productos vencidos bajo cualquier circunstancia</li>
                                <li>Descartar productos vencidos en la basura regular sin seguir protocolos</li>
                                <li>Ignorar las fechas de vencimiento argumentando que "todavía sirven"</li>
                                <li>Modificar o alterar las fechas de vencimiento de los productos</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Botón para ver lotes vencidos -->
            <div class="text-center">
                <p>Para ver el listado completo de lotes vencidos, haga clic en el siguiente botón:</p>
                <a href="@Url.Action("Index", "Lotes", new { soloVencidos = true })" class="btn btn-danger">
                    <i class="fas fa-list me-1"></i>Ver Lotes Vencidos
                </a>
            </div>
        </div>
    </div>
</div>